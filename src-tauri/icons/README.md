# Generated Windows icons

The committed source is `frontend/public/favicon.svg`. Run
`scripts/package-windows.ps1` or `cargo tauri icon` to generate the PNG/ICO
bundle inputs in this directory. Generated binary icons are intentionally
ignored; release builds regenerate them deterministically from the source SVG.
